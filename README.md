# Wolfram NFT Marketplace

## To run the code follow the steps below:

* Download the source from the directory
* Navigate to the directory
* Run "npm install" ("yarn")
* Run "npm start" ("yarn start")